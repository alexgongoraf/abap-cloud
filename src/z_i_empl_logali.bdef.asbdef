managed; // implementation in class zbp_i_empl_logali unique; //comentariado AGF
//strict ( 2 ); //Uncomment this line in order to enable strict mode 2. The strict mode has two variants (strict(1), strict(2)) and is prerequisite to be future proof regarding syntax and to be able to release your BO.
//with draft;
define behavior for z_i_empl_logali alias Employees
persistent table zempl_logali
lock master
 //authorization master ( instance ) //comentariado agf
//etag master <field_name>
{ field ( readonly, numbering : managed ) e_number;
  create ; // create ( authorization : global );
  update;
  delete;
  // field ( readonly, numbering : managed ) e_number; AGF
}